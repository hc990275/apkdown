name: Auto Update & Release (30min)

on:
  schedule:
    # 每 30 分钟运行一次
    - cron: '*/30 * * * *'
  workflow_dispatch: # 允许手动点击按钮触发

defaults:
  run:
    shell: bash

jobs:
  update-and-release:
    runs-on: windows-latest
    permissions:
      contents: write 

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Dependencies
        run: |
          pip install requests pyinstaller

      - name: 🚀 Run Maintenance Script
        id: script
        run: |
          # 脚本会修改版本号和链接，并输出 new_version
          python auto_maintain.py > output.txt
          cat output.txt
          VER=$(grep "::set-output name=new_version::" output.txt | cut -d ':' -f 4)
          echo "NEW_VERSION=$VER" >> $GITHUB_ENV

      - name: 🔨 Build EXE
        run: |
          # 打包 EXE，不显示黑框 (-w)，单文件 (-F)
          pyinstaller -F -w -n "影视更新工具" PY版本.PY

      - name: 🔍 Check for Changes
        id: git-check
        run: |
          # 检查变更 (由于版本号变动，这里必定检测到变化)
          git diff --exit-code || echo "changes_detected=true" >> $GITHUB_OUTPUT

      - name: 💾 Commit & Push Changes
        if: steps.git-check.outputs.changes_detected == 'true'
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add apkdown.sh PY版本.PY CHANGELOG.md
          git commit -m "chore: force update to ${{ env.NEW_VERSION }} [skip ci]"
          git push

      - name: 🔄 Prepare Release Assets
        if: steps.git-check.outputs.changes_detected == 'true'
        run: |
          cp apkdown.sh "软件更新脚本_${{ env.NEW_VERSION }}.sh"

      - name: 🏷️ Create Release
        if: steps.git-check.outputs.changes_detected == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.NEW_VERSION }}
          name: "自动更新 ${{ env.NEW_VERSION }}"
          body: |
            ## 🚀 自动更新日志
            - **版本**: `${{ env.NEW_VERSION }}`
            - **更新策略**: 仅同步 "推薦" 列表中的 32位/Pro/4.x 版本
            
            ### 📂 附件下载
            1. **Windows**: `影视更新工具.exe` (已剔除emu-pro和64位逻辑)
            2. **Android**: `软件更新脚本_...sh`
          files: |
            软件更新脚本_${{ env.NEW_VERSION }}.sh
            dist/影视更新工具.exe
            PY版本.PY
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
