name: Auto Update & Release (30min)

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

defaults:
  run:
    shell: bash

# ✅ 全局设置 Python IO 编码为 UTF-8
env:
  PYTHONIOENCODING: utf-8

jobs:
  update-and-release:
    runs-on: windows-latest
    permissions:
      contents: write 

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Dependencies
        run: pip install requests pyinstaller

      - name: 🚀 Run Maintenance Script
        id: script
        run: |
          python auto_maintain.py > output.txt
          cat output.txt
          VER=$(grep "::set-output name=new_version::" output.txt | cut -d ':' -f 4)
          echo "NEW_VERSION=$VER" >> $GITHUB_ENV

      - name: 🔨 Build EXE
        run: pyinstaller -F -w -n "影视更新工具" PY版本.PY

      - name: 🔍 Check for Changes
        id: git-check
        run: git diff --exit-code || echo "changes_detected=true" >> $GITHUB_OUTPUT

      - name: 💾 Commit & Push Changes
        if: steps.git-check.outputs.changes_detected == 'true'
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add apkdown.sh PY版本.PY CHANGELOG.md
          git commit -m "chore: force update to ${{ env.NEW_VERSION }} [skip ci]"
          git push

      - name: 🔄 Prepare Release Assets
        if: steps.git-check.outputs.changes_detected == 'true'
        run: cp apkdown.sh "软件更新脚本_${{ env.NEW_VERSION }}.sh"

      - name: 🏷️ Create Release
        if: steps.git-check.outputs.changes_detected == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.NEW_VERSION }}
          name: "自动更新 ${{ env.NEW_VERSION }}"
          body: |
            ## 🚀 自动更新日志
            - **版本**: `${{ env.NEW_VERSION }}`
            - **说明**: 自动同步 "推薦" 列表
            
            ### 📂 附件
            - `影视更新工具.exe` (Windows)
            - `软件更新脚本_...sh` (Android)
          files: |
            软件更新脚本_${{ env.NEW_VERSION }}.sh
            dist/影视更新工具.exe
            PY版本.PY
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
